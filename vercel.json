{
  "framework": "vite",
  "buildCommand": "cd flat && npm run build",
  "outputDirectory": "flat/dist",
  "installCommand": "cd flat && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}